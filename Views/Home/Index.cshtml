@{
    ViewBag.Title = "بیمه پاسارگاد";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="row">
    <div>
        <h2>لیست نظرسنجی ها</h2>
        @foreach (var item in Model)
        {
           @Html.ActionLink((string)item.Title, "Index", "Questionnaire", 
               new {questionnaireId = item.Id, needLogin = @item.NeedLogin}, null)
           <br/>
        }
    </div>
</div>